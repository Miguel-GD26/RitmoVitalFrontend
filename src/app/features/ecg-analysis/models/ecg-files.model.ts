export interface EvaluationFiles {
  datFile: File;
  heaFile: File;
  atrFile: File;
  pacienteId?: number;
}

export interface ProductionFiles {
  datFile: File;
  heaFile: File;
  pacienteId?: number;
}
